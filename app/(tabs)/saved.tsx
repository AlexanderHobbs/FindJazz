import {View, Text, FlatList, StyleSheet } from 'react-native';
import styles from '../../themes/styles';

export default function SavedTab(){
    return (
        <View style = {styles.container}>
            <Text>Save locations to swoop back around later</Text>
        </View>

    );
}

